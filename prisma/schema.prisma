generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Champion {
  id        String   @id @default(cuid())
  key       String   @unique
  name      String
  title     String
  tags      String[]
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("champions")
}

model RuneBuild {
  id                String   @id @default(cuid())
  championKey       String?
  championName      String?
  gameMode          String
  primaryTreeId     Int
  primaryTreeName   String
  secondaryTreeId   Int
  secondaryTreeName String
  primaryRunes      Json
  secondaryRunes    Json
  statShards        Json
  selectedItems1    Json     @default("[]")
  selectedItems2    Json     @default("[]")
  selectedSpells    Json     @default("[]")
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  @@map("rune_builds")
}
